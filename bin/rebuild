#!/bin/bash -ex                                                                                                    
docker-compose stop; docker-compose rm -f ; docker-compose build; docker-compose up --force-recreate 
